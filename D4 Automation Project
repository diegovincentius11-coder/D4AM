local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Window = Rayfield:CreateWindow({
   Name = "Adopt Me Automation by D4for",
   Icon = 0, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
   LoadingTitle = "Loading User Interface",
   LoadingSubtitle = "by D4for",
   ShowText = "D4 Automation", -- for mobile users to unhide Rayfield, change if you'd like
   Theme = "Ocean", -- Check https://docs.sirius.menu/rayfield/configuration/themes

   ToggleUIKeybind = "K", -- The keybind to toggle the UI visibility (string like "K" or Enum.KeyCode)

   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false, -- Prevents Rayfield from emitting warnings when the script has a version mismatch with the interface.

   -- ScriptID = "sid_xxxxxxxxxxxx", -- Your Script ID from developer.sirius.menu — enables analytics, managed keys, and script hosting

   ConfigurationSaving = {
      Enabled = true,
      FolderName = "D4Automation", -- Create a custom folder for your hub/game
      FileName = "D4"
   },

   Discord = {
      Enabled = false, -- Prompt the user to join your Discord server if their executor supports it
      Invite = "noinvitelink", -- The Discord invite code, do not include Discord.gg/. E.g. Discord.gg/ABCD would be ABCD
      RememberJoins = true -- Set this to false to make them join the Discord every time they load it up
   },

   KeySystem = true, -- Set this to true to use our key system
   KeySettings = {
      Title = "D4 Automation Dev Version",
      Subtitle = "Key System",
      Note = "This script only for dev use,not for public use",
      FileName = "KeyD4", -- -- Use this to tell the user how to get a key It is recommended to use something unique, as other scripts using Rayfield may overwrite your key file
      SaveKey = true, -- The user's key will be saved, but if you change the key, they will be unable to use your script
      GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
      Key = {"d4ams"} -- List of keys that the system will accept, can be RAW file links (pastebin, github, etc.) or simple strings ("hello", "key22")
   }
})
local Tab = Window:CreateTab("Main Menu", "Home") -- Title, Image
local running = false

local Toggle = Tab:CreateToggle({
   Name = "Main Automation",
   CurrentValue = false,
   Flag = "Toggle1", -- A flag is the identifier for the configuration file; make sure every element has a different flag if you're using configuration saving to ensure no overlaps
   Callback = function(Value)
      if Value then
         running = true
         spawn(function()
            while running do
               local args = {
                  "2_857ba6dd96824623babdca65a30cce68",
                  {
                     use_sound_delay = true,
                     equip_as_last = false
                  }
               }
               game:GetService("ReplicatedStorage"):WaitForChild("API"):WaitForChild("ToolAPI/Equip"):InvokeServer(unpack(args))
               local args = {
                  "2_857ba6dd96824623babdca65a30cce68",
                  "START"
               }
               local Players = game:GetService("Players")
               local player = Players.LocalPlayer
               local character = player.Character or player.CharacterAdded:Wait()
               local hrp = character:WaitForChild("HumanoidRootPart")

               local args = {
                  hrp.CFrame
               }

               game:GetService("ReplicatedStorage")
                   :WaitForChild("API")
                   :WaitForChild("ToolAPI/ServerUseTool")
                   :InvokeServer(unpack(args))
               game:GetService("ReplicatedStorage"):WaitForChild("API"):WaitForChild("PlaceableToolAPI/CreatePlaceable"):InvokeServer(unpack(args))
               game:GetService("ReplicatedStorage"):WaitForChild("API"):WaitForChild("HousingAPI/GetNonTradeableFurniture"):InvokeServer()
          game:GetService("ReplicatedStorage"):WaitForChild("API"):WaitForChild("HousingAPI/ListHouse"):InvokeServer()
               print("Magic door tidak tedeteksi,memasang magic door...")     

               wait(60)
            end
         end)
      else
         running = false
      end
   -- The function that takes place when the toggle is pressed
   -- The variable (Value) is a boolean on whether the toggle is true or false
   end,
})

local TextChatService = game:GetService("TextChatService")
local chatEnabled = false
local chatMessage = ""
local chatCooldown = 30

local chatInput = Tab:CreateInput({
   Name = "Insert text for Chat Automation",
   CurrentValue = "",
   PlaceholderText = "Example : Hai Sayang",
   RemoveTextAfterFocusLost = false,
   Flag = "Input1",
   Callback = function(Text)
      -- The function that takes place when the input is changed
      -- The variable (Text) is a string for the value in the text box
      chatMessage = Text
   end,
})

local cooldownInput = Tab:CreateInput({
   Name = "Cooldown Chat Automation",
   CurrentValue = "30",
   PlaceholderText = "put in seconds",
   RemoveTextAfterFocusLost = false,
   Flag = "Input2",
   Callback = function(CD)
      -- The function that takes place when the input is changed
      -- The variable (CD) is a string for the value in the text box
      local value = tonumber(CD)
      if value and value >= 1 then
         chatCooldown = value
      else
         chatCooldown = 30
      end
   end,
})

local Toggle = Tab:CreateToggle({
   Name = "Chat Automation",
   CurrentValue = false,
   Flag = "Toggle2", -- A flag is the identifier for the configuration file; make sure every element has a different flag if you're using configuration saving to ensure no overlaps
   Callback = function(Value)
      chatEnabled = Value
      if not chatEnabled then
         return
      end

      task.spawn(function()
         while chatEnabled do
            if chatMessage ~= "" then
               pcall(function()
                  local channel = TextChatService.TextChannels:FindFirstChild("RBXGeneral")
                  if channel then
                     channel:SendAsync(chatMessage)
                  else
                     local Players = game:GetService("Players")
                     local player = Players.LocalPlayer
                     if player then
                        player:Chat(chatMessage)
                     end
                  end
               end)
            end

            task.wait(chatCooldown)
         end
      end)
   end,
})
local Tab = Window:CreateTab("Misc", "Wrench") -- Title, Image
local Button = Tab:CreateButton({
   Name = "Hop to Crowded Server",
   Callback = function()
  local Players = game:GetService("Players")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")

local TARGET_MIN_PLAYERS = 34 -- Change this to your preferred crowd size
local HOP_DELAY = 1

local player = Players.LocalPlayer

local function getServerPlayerCount(server)
	if not server then
		return 0
	end

	return tonumber(server.playing or server.playerCount or server.players or server.playercount or 0) or 0
end

local function getBestServerCandidate()
	local ok, response = pcall(function()
		return HttpService:GetAsync(string.format(
			"https://games.roblox.com/v1/games/%d/servers/Public?sortOrder=Asc&limit=100",
			game.PlaceId
		))
	end)

	if not ok then
		warn("Could not fetch server list: " .. tostring(response))
		return nil, 0
	end

	local success, decoded = pcall(function()
		return HttpService:JSONDecode(response)
	end)

	if not success or not decoded or not decoded.data then
		warn("Received an invalid server list response.")
		return nil, 0
	end

	local bestServer = nil
	local bestCount = 0

	for _, server in ipairs(decoded.data) do
		if server and server.id ~= game.JobId then
			local count = getServerPlayerCount(server)
			if count > bestCount then
				bestCount = count
				bestServer = server
			end
		end
	end

	return bestServer, bestCount
end

local function tryOneHop()
	if not player then
		return
	end

	if Players.NumPlayers >= TARGET_MIN_PLAYERS then
		print("Already in a crowded server with " .. Players.NumPlayers .. " players.")
		return
	end

	print("Current server has only " .. Players.NumPlayers .. " players. Looking for a busier server...")
	task.wait(HOP_DELAY)

	local bestServer, bestCount = getBestServerCandidate()

	if bestServer and bestServer.id and bestCount >= TARGET_MIN_PLAYERS then
		print(string.format("Found a server with %d players. Teleporting...", bestCount))
		TeleportService:TeleportToPlaceInstance(game.PlaceId, bestServer.id, player)
		return
	end

	if bestServer and bestServer.id then
		print(string.format("No server met the target threshold. Best available server has %d players. Teleporting anyway...", bestCount))
		TeleportService:TeleportToPlaceInstance(game.PlaceId, bestServer.id, player)
		return
	end

	print("Could not fetch a server list. Falling back to a regular hop.")
	TeleportService:Teleport(game.PlaceId, player)
end

tryOneHop() 
   -- The function that takes place when the button is pressed
   end,
})

local Toggle = Tab:CreateToggle({
   Name = "Anti AFK",
   CurrentValue = true,
   Flag = "Toggle1", -- A flag is the identifier for the configuration file; make sure every element has a different flag if you're using configuration saving to ensure no overlaps
   Callback = function(Value1)
      if Value1 then
         local vu = game:GetService("VirtualUser")
         game:GetService("Players").LocalPlayer.Idled:connect(function()
            vu:Button2Down(Vector2.new(0, 0), workspace.CurrentCamera.CFrame)
            wait(1)
            vu:Button2Up(Vector2.new(0, 0), workspace.CurrentCamera.CFrame)
         end)
      end
   -- The function that takes place when the toggle is pressed
   -- The variable (Value) is a boolean on whether the toggle is true or false
   end,
})

local Tab = Window:CreateTab("Information", "info") -- Title, Image
local Paragraph = Tab:CreateParagraph({Title = "Script Information", Content = "Version : 0.2.1"})
local Paragraph = Tab:CreateParagraph({Title = "Latest Changelog", Content = "Version 0.2.1 : Improved Hop to Crowd Server logic"})
